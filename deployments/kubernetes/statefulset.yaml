---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: discord
  namespace: shuudann
  labels:
    app: discord
spec:
  replicas: 10
  selector:
    matchLabels:
      app: discord
  serviceName: discord
  template:
    metadata:
      labels:
        app: discord
    spec:
      terminationGracePeriodSeconds: 30
      containers:
        - name: discord
          image: registry.digitalocean.com/lee-aaron-reg/reaper-discord
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 9000